{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with MSXgl",
            "type": "shell",
            "command": "${workspaceFolder}\\build.bat",
            "args": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}